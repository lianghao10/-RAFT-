# Weights

Place local RAFT checkpoints here, for example:

```text
weights/raft-things.pth
weights/raft-sintel.pth
```

If `--raft_weights` is not provided, the program uses torchvision's public pretrained RAFT weights.
